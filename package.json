{
  "name": "coffee-pos-web-final",
  "version": "1.4.0",
  "private": true,
  "description": "Hardened Coffee POS web migration with ingredient-aware availability tooltips, recipe-required product gating, detailed transaction audit viewer, reliable checkout reset, responsive admin UI, safe retry protection, exact stock reversal, backups, reports and PostgreSQL support.",
  "scripts": {
    "start": "node --no-warnings server.js",
    "start:postgres": "node scripts/start-postgres.js",
    "smoke": "node --no-warnings scripts/smoke-test.js",
    "test:audit-view": "node scripts/audit-view-test.js",
    "start:easy": "node scripts/start-pos.js",
    "test:availability": "node scripts/availability-test.js"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "engines": {
    "node": ">=22.5"
  }
}
