{
  "name": "coffee-pos-web-final",
  "version": "1.5.0",
  "private": true,
  "description": "Coffee POS web migration with live cross-terminal refresh, filtered audit logs, version-aware What's New notices, batch replenish/transfer, ingredient-aware availability, reports, backups and PostgreSQL support.",
  "scripts": {
    "start": "node --no-warnings server.js",
    "start:postgres": "node scripts/start-postgres.js",
    "smoke": "node --no-warnings scripts/smoke-test.js",
    "test:audit-view": "node scripts/audit-view-test.js",
    "start:easy": "node scripts/start-pos.js",
    "test:availability": "node scripts/availability-test.js"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "engines": {
    "node": ">=22.5"
  }
}
